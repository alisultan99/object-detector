export const metadata = {
  title: 'Live Object Detector',
  description: 'AI Video Object Detection App',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
